datasource db {
  provider = "postgresql"
  url      = env("DATABASE_URL")
}

generator exam_client {
  provider = "prisma-client-js"
  output   = "../exams/node_modules"
}

generator web_client {
  provider = "prisma-client-js"
  output   = "../web/node_modules"
}

model Exercise {
  id          Int       @id @default(autoincrement())
  difficulty  Float
  prompt      String
  choices     String[]
  correct     Int[]
  directive   Directive @relation(fields: [directiveId], references: [id])
  directiveId Int
}

model Directive {
  id        Int           @id @default(autoincrement())
  prompt    String
  type      DirectiveType
  exercises Exercise[]
}

enum DirectiveType {
  SELECT
  SELECT_OR_FILL
  ORDERED_SELECT
  SHORT_ANSWER
  LISTENING
}
